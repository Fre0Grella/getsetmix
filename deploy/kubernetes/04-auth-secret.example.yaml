# Optional: apply this if you want a static token.
# IMPORTANT: replace the value before applying.
apiVersion: v1
kind: Secret
metadata:
  name: getsetmix-auth
type: Opaque
stringData:
  token: "REPLACE_ME"
